What the core is (ISA, 5-stage pipeline, static not-taken predictor, forwarding, stalls).

Block diagram (even an ASCII one is fine).

How to:

- build/run testbench

- view waveforms

Examples of programs you ran (screenshots of console or GTKWave).
